This folder contains all globally scoped (used accross the project) enums (a "list" of
named values far variables).
Enums are used externally as type int (see chunk.gd for example), since under the hood
of GDscript it is just putting a name to an index value.
