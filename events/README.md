# Events

Same pattern as ailments. Each event is a self-contained folder extending `BaseEvent`
(`res://scripts/systems/base_event.gd`).

```
events/
  red_light_run/
    red_light_run.tscn
    red_light_run.gd
```
